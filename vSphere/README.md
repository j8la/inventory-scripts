# vSphere inventory scripts

  + *ClassicMethod*
  Classic method with dynamic inventory. Can be updated with Tower Rest API.
  + *JobMethod*
  Uses a job template for generate the inventory with a Git repository to keep code up to date 